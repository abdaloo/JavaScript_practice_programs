// Problem: Given `length` and `width`, calculate and log the area of the rectangle.
// A = w * l

let length = 12;
let width = 10;
let Area = width * length;
console.log(`The rectangle of ${length} & ${width} is: ${Area}`);